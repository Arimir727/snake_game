import 'package:flame/components.dart';

final kTileSize = Vector2.all(20);
final kColNum = 28;
final kRowNum = 28;
final kGameSize = Vector2.all(600);

// final gameSize = Vector2.all(600);
//     cameraComponent.viewfinder.visibleGameSize = gameSize;
//     cameraComponent.viewfinder.anchor = Anchor.topLeft;